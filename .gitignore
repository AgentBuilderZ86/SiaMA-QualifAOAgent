.next
node_modules
out
dist
.env
.env.local
.env.*.local
data/ao-cache.json
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Local Netlify folder
.netlify

# Build incremental cache
*.tsbuildinfo

# Local scratch dirs / zips (ne pas versionner)
.tmp-*/
.tmp-*.zip

# Artefacts locaux (générés / exports) et cache Python — hors dépôt (scan Netlify + taille)
output/
scripts/__pycache__/
paddle_ocr_service/.venv
paddle_ocr_service/__pycache__/

# Fichiers temporaires Office (verrou)
~$*
